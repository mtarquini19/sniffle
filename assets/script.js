$(document).ready(function () {

    // assign variables for date & city search storage
    var todaysDate = moment().format("MMM Do YYYY");
    var cities = $(".list-group");
    var cityButtons = [];

    // display current weather
    // var displayWeather = (cityName) function() {

    });
